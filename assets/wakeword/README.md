# Wake-word models

Production wake-word model files. Loaded by
`packages/agent/src/lafufu_agent/wakeword.py` (`OpenWakeWordDetector`).
Drop trained `.onnx` files here and point `LAFUFU_WAKEWORD_MODEL` at them.
